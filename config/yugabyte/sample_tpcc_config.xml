<?xml version="1.0"?>
<parameters>

    <!-- Connection details -->
    <type>YUGABYTE</type>
    <driver>com.yugabyte.Driver</driver>
    <url>jdbc:yugabytedb://localhost:5433/yugabyte?sslmode=disable&amp;load-balance=true&amp;ApplicationName=tpcc&amp;reWriteBatchedInserts=true</url>
    <username>yugabyte</username>
    <password></password>
    <isolation>TRANSACTION_REPEATABLE_READ</isolation>
    <batchsize>128</batchsize>
    <loaderThreads>16</loaderThreads>

    <!-- Retry configuration for connection/transient errors -->
    <!-- retryDurationSeconds: Max time to retry connection errors (default: 14400 = 4 hours) -->
    <!-- Set to 0 to disable time-based retry and use count-based retry only -->
    <retryDurationSeconds>14400</retryDurationSeconds>
    <!-- retryInitialBackoffMs: Initial backoff between retries (default: 1000ms) -->
    <retryInitialBackoffMs>1000</retryInitialBackoffMs>
    <!-- retryMaxBackoffMs: Maximum backoff between retries (default: 30000ms = 30s) -->
    <retryMaxBackoffMs>30000</retryMaxBackoffMs>
    <!-- retries: Max retry count for non-connection errors (default: 3) -->
    <retries>3</retries>

    <!-- Scale factor is the number of warehouses in TPCC -->
    <scalefactor>1</scalefactor>

    <!-- The workload -->
    <terminals>1</terminals>
    <works>
        <work>
            <time>60</time>
            <rate>10000</rate>
            <weights>45,43,4,4,4</weights>
        </work>
    </works>

    <!-- TPCC specific -->
    <transactiontypes>
        <transactiontype>
            <name>NewOrder</name>
            <!--<preExecutionWait>18000</preExecutionWait>-->
            <!--<postExecutionWait>12000</postExecutionWait>-->
        </transactiontype>
        <transactiontype>
            <name>Payment</name>
            <!--<preExecutionWait>3000</preExecutionWait>-->
            <!--<postExecutionWait>12000</postExecutionWait>-->
        </transactiontype>
        <transactiontype>
            <name>OrderStatus</name>
            <!--<preExecutionWait>2000</preExecutionWait>-->
            <!--<postExecutionWait>10000</postExecutionWait>-->
        </transactiontype>
        <transactiontype>
            <name>Delivery</name>
            <!--<preExecutionWait>2000</preExecutionWait>-->
            <!--<postExecutionWait>5000</postExecutionWait>-->
        </transactiontype>
        <transactiontype>
            <name>StockLevel</name>
            <!--<preExecutionWait>2000</preExecutionWait>-->
            <!--<postExecutionWait>5000</postExecutionWait>-->
        </transactiontype>
    </transactiontypes>
</parameters>
